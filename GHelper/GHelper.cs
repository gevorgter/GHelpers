using Microsoft.IO;

namespace GHelpers
{
    public static class GHelper
    {
        public static RecyclableMemoryStreamManager _recyclableMemoryStreamManager = new RecyclableMemoryStreamManager();
    }
}
